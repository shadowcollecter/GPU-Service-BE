# （可选）用 ConfigMap 存放 MinIO endpoint 和 bucket 名称
# filepath: minio-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: minio-config
  namespace: gpu-service
data:
  MINIO_ENDPOINT: "http://192.168.194.217:9000"
  MINIO_BUCKET:   "notebooks"